from django.contrib import admin
from . models import React, CropModel1, CropModel2
# Register your models here.
admin.site.register(React)
admin.site.register(CropModel1)
admin.site.register(CropModel2)